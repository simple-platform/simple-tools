{
  "name": "@simple/scl-parser",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "mix credo --strict",
    "test": "mix test --cover",
    "build:release": "mix release --overwrite",
    "deps": "mix deps.get"
  }
}
