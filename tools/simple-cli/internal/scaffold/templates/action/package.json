{
  "name": "@{{.Scope}}/action-{{.ActionName}}",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "source": "src/index.ts",
  "main": "build/release.wasm",
  "scripts": {
    "build:setup": "rm -rf build && mkdir -p build",
    "build:js": "esbuild src/index.ts --bundle --minify --outfile=./build/bundle.js",
    "build": "npm-run-all -s build:setup build:js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@simpleplatform/sdk": "^1.0.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.4",
    "esbuild": "0.27.3",
    "npm-run-all2": "5.0.2",
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
