{
  "name": "@simple/scl-parser-cli",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "mix credo",
    "test": "mix test",
    "build": "mix compile",
    "tidy": "mix deps.clean --unlock --unused",
    "deps": "mix deps.get"
  }
}
