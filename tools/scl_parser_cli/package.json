{
  "name": "@simple/scl-parser-cli",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "mix credo",
    "test": "mix test --no-start",
    "build": "mix compile",
    "tidy": "mix deps.clean --unlock --unused",
    "deps": "mix deps.get"
  }
}
